<form method="post" action="{{ path('prestataire_accepter', {'id': prestation.id}) }}" onsubmit="return confirm('êtes-vous sur de vouloir réaliser cette prestation?'); ">
    <input type="hidden" name="_method" value="GET">
    <input type="hidden" name="_token" value="{{ csrf_token('accepter' ~ prestation.id) }}">
    <button class="btn">Je souhaite réaliser cette prestation</button>
</form>
